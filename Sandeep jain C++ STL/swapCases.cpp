#include<bits/stdc++.h>
#include<string.h>

using namespace std;

// void swapCase(char str[]) {
//   int size = strlen(str);
//   for(int i = 0; i < size; i++) {
//     if()
//   }
// }

int main() {
  char alpha;
  cout << "Enter alphabet: ";
  cin >> alpha;
  cout << toupper_l(alpha) << endl;
  cout << alpha << " " << alpha + 1 << endl;
  return 0;
}
