#include<bits/stdc++.h>
using namespace std;
int main() {
  int size;
  cout << "Enter size: ";
  cin >> size;
  int two_d_arr[size][size];
  for(int i = 0; i < size; i++) {
    for(int j = 0; j < size; j++) {
      cin >> two_d_arr[i][j];
    }
  }
  int arr[size];
  for(int i = 0; i < size; i++) {
    for(int j = 0; j < size; j++) {

    }
  }
}
